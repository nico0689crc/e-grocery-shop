const routes = {
  pages: {
    public: {
      home: '/',
      cookies_policy: '/cookies-policy',
    },
    authenticated: {
      dashboard: '/dashboard',
    },
  },
};

export const apiRoutes = {};

export default routes;
